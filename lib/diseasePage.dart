import 'package:flutter/material.dart';

class DiseasePage extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(title: Text("Scan Penyakit"),),
      body: Center(),
      );
    }
}